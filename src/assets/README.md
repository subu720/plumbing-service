Place the provided Patra logo image here as `patra-logo.png`.

Recommended sizes and usage:
- Desktop header: 40x40 to 56x56px (export at 2x or 3x for retina)
- Favicon: 32x32 and 16x16
- Hero/large use: 240px wide

Filename: `src/assets/patra-logo.png`

Steps to add the logo:
1. Save the attached image from the chat to `src/assets/patra-logo.png`.
2. Run the dev server (`pnpm dev` or `npm run dev`) and verify the header shows the new logo.

If you want, I can embed the image directly into the repo as a base64 PNG—tell me if you'd like that and I'll add it for you.
